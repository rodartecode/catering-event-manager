# Specs

This change does not introduce or modify any capabilities.

It is a CI configuration fix that removes duplicate pnpm version specifications to resolve the `ERR_PNPM_BAD_PM_VERSION` error from `pnpm/action-setup@v4`.

No spec files are required.
